import React from 'react';

const WeatherInfo = ({ data }) => {
  return (
    <div className="sub-container user-info-containers active">
      {/* Display Weather Information */}
    </div>
  );
};

export default WeatherInfo;
